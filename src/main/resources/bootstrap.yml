server:
  port: 8080

spring:
  application:
  name: spring-cloud-config-client
#  profiles:
#    active: development
  cloud:
    config:
      uri: http://localhost:8888

management:
  secrity:
    enabled: false
